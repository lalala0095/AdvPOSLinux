# # Redirect HTTP to HTTPS
# server {
#     listen 80;
#     server_name expensechatbot.store www.expensechatbot.store;

#     # Redirect all traffic to HTTPS
#     return 301 https://$host$request_uri;
# }

# # Serve the app over HTTPS
# server {
#     listen 443 ssl;
#     server_name expensechatbot.store www.expensechatbot.store;

#     # SSL configuration
#     # ssl_certificate /etc/letsencrypt/live/expensechatbot.store/fullchain.pem;
#     # ssl_certificate_key /etc/letsencrypt/live/expensechatbot.store/privkey.pem;
    
#     # # SSL settings for stronger security
#     # ssl_protocols TLSv1.2 TLSv1.3;
#     # ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256';
#     # ssl_prefer_server_ciphers on;

#     # Location block for the Flask app
#     location / {
#         proxy_pass http://127.0.0.1:5000;  # Assuming Flask app runs on port 5000
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Additional SSL settings and error handling can be added here
# }
